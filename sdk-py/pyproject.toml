[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "garp-sdk"
version = "0.1.0"
description = "Python SDK for GARP participant-node JSON-RPC"
requires-python = ">=3.10"
dependencies = ["httpx>=0.24.0"]