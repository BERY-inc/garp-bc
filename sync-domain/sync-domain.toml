# Sync Domain Configuration

domain_id = "domain-1"
database_url = "postgresql://garp:garp@localhost:5432/garp"

[api]
host = "0.0.0.0"
port = 8082

[kafka]
bootstrap_servers = "localhost:9092"

[storage]
redis_url = "redis://localhost:6379"

[logging]
level = "info"

[genesis]
chain_id = "garp-devnet"
genesis_time = "2024-01-01T00:00:00Z"
initial_validators = ["participant-1"]

[chain]
slot_duration_ms = 1000
epoch_length = 60
rotation_interval_slots = 60