# --- Database ---
POSTGRES_DB=garp
POSTGRES_USER=garp
POSTGRES_PASSWORD=change_me

# Connection URLs
DATABASE_URL=postgresql://garp:${POSTGRES_PASSWORD}@postgres:5432/garp
REDIS_URL=redis://redis:6379
KAFKA_BROKERS=kafka:9092

# Synchronizer / Nodes
NODE_ID=sync-1
PARTICIPANT_URL=http://participant-node:8090
SYNCHRONIZER_URL=http://global-synchronizer:8000

# Backend / Gateway
BACKEND_URL=http://backend-go:8081
JWT_SECRET=change_me
RATE_LIMIT_RPM=100

# Optional domain for reverse proxy (if using external Nginx/Caddy)
PUBLIC_DOMAIN=example.com